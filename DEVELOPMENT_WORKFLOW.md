# JustGO Godot 版本重构开发流程

## 1. 重构目标

当前版本最大的问题不是素材数量不足，而是把完整静态图片直接作为人物、武器和技能贴图进行平移、旋转，导致动作缺乏生命力，枪口、弹道和技能表现也无法自然衔接。

本轮重构遵循以下原则：

1. **先把玩法、手感和逻辑做正确，再制作正式美术。**
2. **所有未完成的视觉对象统一使用简单几何图形占位。** 人物先用方块，怪物用不同颜色和大小的圆形或多边形，子弹用短线或小圆点。
3. **一次只完成一个系统。** 当前系统通过验收后，才进入下一个系统。
4. **素材按需制作。** 先确定尺寸、动画帧、朝向、锚点和用途，再生成或寻找对应素材，禁止一次性把大量图片塞入项目。
5. **逻辑层与表现层分离。** 替换人物或技能动画时，不得重新修改移动、伤害、碰撞等核心逻辑。
6. **每次改动保持可运行。** 不提交解析失败、场景打不开或明显未完成的中间状态。
7. **每个通过检查的原子改动自动提交并推送到 `origin/main`。**

## 2. 本次视觉重置范围

下一阶段将从 Godot 项目的运行内容中移除以下静态大图依赖：

- 玩家人物贴图；
- 怪物和 Boss 静态贴图；
- 枪械、环刃、火球、落雷等直接平移或旋转的完整图片；
- 为上述图片服务的临时遮罩 Shader 和运行时裁切逻辑；
- 尚未经过规格确认、但已经提前导入的展示图素材。

项目外部的原始素材目录不做删除。Godot 项目内不再使用的素材，在确认没有引用后从版本库移除，避免继续进入导出包。

重置后的第一版只保留：

- 玩家：一个带朝向提示的方块；
- 枪械：一条可围绕玩家旋转的矩形；
- 枪口：独立 `Marker2D`；
- 子弹：从枪口发射的小矩形或圆点；
- 怪物：按类型区分颜色、大小和轮廓的几何图形；
- 环刃：旋转的菱形或弧线占位；
- 落雷：预警圆环、闪电线和命中区域占位；
- 经验、伤害、碰撞、升级和 UI：继续保留并逐项验证。

## 3. 技术结构约定

### 3.1 玩家

- 根节点使用 `CharacterBody2D`，只负责移动和碰撞。
- `VisualRoot` 只负责玩家外观，初期为几何方块，后期可替换为 `AnimatedSprite2D`。
- `WeaponPivot` 根据目标方向旋转。
- 武器放在 `WeaponPivot` 下，枪口使用独立 `Marker2D`。
- 子弹必须从枪口的全局坐标生成，不能从玩家中心生成后再做视觉偏移。
- 移动停止时速度应立即归零或按明确参数减速，不允许出现无设计依据的漂移。

### 3.2 怪物和 Boss

- 移动、生命、攻击、掉落和外观分别处理。
- 怪物逻辑不得依赖某张贴图的实际尺寸。
- 碰撞体尺寸由配置明确指定，不从原始图片透明边缘推断。
- Boss 可以拥有独立技能状态机，但仍使用统一伤害接口。

### 3.3 武器和技能

- 每类武器拥有独立配置与行为脚本。
- 伤害判定与视觉特效分离；关闭特效后，武器仍必须正常造成伤害。
- 特效节点必须设置寿命、对象池或数量上限，禁止无限累计。
- 十层构筑的质变优先通过颜色、轨迹、尺寸、攻击方式或行为变化表现，不通过无限叠加粒子数量表现。

### 3.4 美术替换接口

每个实体均保留统一的 `VisualRoot`。正式素材只能替换该节点下的表现内容，不改变：

- 角色根节点；
- 碰撞体；
- 枪口和技能发射点；
- 移动与伤害脚本；
- 数值配置；
- 存档或升级数据。

## 4. 分阶段开发计划

### 阶段 0：基线确认与视觉清理

工作内容：

- 记录当前可运行入口、场景结构和核心功能；
- 找出所有静态大图、遮罩 Shader 及其引用；
- 移除运行时静态图依赖；
- 用几何图形替换玩家、怪物、Boss、枪械和技能；
- 保证项目仍可进入战斗。

验收标准：

- 项目无 Parser Error 和缺失资源错误；
- 画面中不再出现现有静态人物、怪物、环刃、火球和落雷图片；
- 玩家能够移动，怪物能够生成，基础攻击能够造成伤害；
- 场景至少连续运行 10 分钟且节点数量不会无上限增长。

本阶段不制作任何正式美术。

### 阶段 1：移动、镜头与手机轮盘

工作内容：

- 调整移动加速、减速和停止响应；
- 解决方向键或轮盘松开后的漂移；
- 统一键盘测试输入与手机触摸输入的数据通道；
- 调整横屏轮盘位置、半径、死区和触摸范围；
- 校准镜头跟随和无限地图循环表现。

验收标准：

- 松开轮盘后人物响应符合设定，没有持续漂移；
- 八方向移动速度一致；
- 轮盘不会遮挡关键 UI；
- 720p、1080p 横屏比例下均可操作。

本阶段仍使用方块人物，不申请人物素材。

### 阶段 2：枪械、瞄准与弹道

工作内容：

- 自动选择合理目标；
- 武器围绕人物正确转向；
- 枪口位置随武器旋转；
- 子弹从枪口发射；
- 完成射速、射程、穿透、散射和暴击的基础接口。

验收标准：

- 武器方向、枪口和子弹轨迹一致；
- 目标死亡或离开范围后可以安全切换目标；
- 高射速下不会出现明显节点堆积；
- 逻辑在无正式枪械贴图时完整可玩。

通过后才确定第一把枪的正式素材规格。

### 阶段 3：怪物、碰撞与成长节奏

工作内容：

- 校准怪物追踪、分离、围堵和接触伤害；
- 恢复生命、护盾、防御和受击无敌帧；
- 校准经验需求递增和拾取范围；
- 实现怪物数量、生命、攻击力随时间增长；
- 13 分钟后进入深夜并提高难度。

验收标准：

- 玩家只在生命值实际归零时死亡；
- 护盾优先承受伤害，UI 与实际数值一致；
- 怪物不会完全重叠成一个点，也不会因避障产生严重抖动；
- 难度曲线可配置，无硬编码散落。

通过后逐类确定小怪动画素材。

### 阶段 4：升级三选一与构筑系统

工作内容：

- 升级时暂停战斗并随机提供三个选项；
- 武器、武器强化和数值牌进入统一但可控的随机池；
- 武器和强化具有数量限制；
- 数值牌不设持有数量上限；
- 塔罗牌最多五张，替换界面可查看说明并可关闭返回；
- 构筑最多十层，第十层触发行为或配色质变。

验收标准：

- 选择界面不会卡死，重复点击不会重复结算；
- 所有随机池在候选不足时都有安全降级；
- 十层以后不会继续创建额外特效节点；
- 暂停和恢复状态严格配对。

### 阶段 5：逐件实现武器

武器严格按以下顺序逐个开发，每完成一个才进入下一个：

1. 基础枪械；
2. 环刃；
3. 火球或爆炸类武器；
4. 落雷；
5. 后续新增武器。

每件武器都依次经历：

1. 几何占位实现玩法；
2. 数值与性能验收；
3. 编写该武器素材规格；
4. 只生成或寻找这一件武器所需素材；
5. 接入动画并校准锚点；
6. 手机实机验收；
7. 提交并推送。

### 阶段 6：Boss、深夜与终局

工作内容：

- 每两分钟生成一个小 Boss，未击杀的 Boss 不阻止下一只出现；
- Boss 类型循环或按时间解锁，避免重复感；
- 13 分钟进入深夜；
- 25 分钟出现最终 Boss；
- 最终 Boss 数值极高，但保留理论击败可能；
- 明确显示当前 Boss 与下一只 Boss 的信息。

验收标准：

- 多 Boss 同场不会破坏目标选择和 UI；
- Boss 技能拥有预警和可规避窗口；
- 最终 Boss 不依靠直接锁死玩家实现难度；
- 25 分钟压力测试无显著持续掉帧。

通过玩法验收后，再逐个制作 Boss 动画，不一次导入全部 Boss 图片。

### 阶段 7：音频、特效、复活与 Android

工作内容：

- 接入背景音乐、射击音、受击音和 Boss 音效；
- 采用对象池、粒子预算和同类音效限流；
- 接入死亡后激励视频复活，并恢复 10 点生命值；
- 完成横屏、自适应安全区、暂停恢复和 Android 导出；
- 进行低端安卓设备性能测试。

验收标准：

- 广告失败、取消或 SDK 不可用时不会黑屏或卡死；
- 复活只触发一次且状态完整恢复；
- 长时间运行内存和节点数量稳定；
- APK 可以安装、启动、切后台并恢复。

## 5. 素材制作门槛与规格

任何素材生成前，必须先填写本节模板并确认占位版本已经通过验收。

### 5.1 通用要求

- 像素风格和色板保持统一；
- 使用透明背景 PNG 或明确的 Sprite Sheet；
- 不得包含展示用渐变背景、文字、水印、阴影画布或大面积无效透明边距；
- 图片尺寸尽量使用 2 的幂或紧凑图集；
- Godot 导入关闭过滤，使用最近邻缩放；
- 明确锚点、朝向、枪口点、受击点和碰撞体建议；
- 正式素材必须有动作，不能只把一张立绘当作移动动画。

### 5.2 推荐规格

| 类型 | 建议尺寸 | 最低动画要求 |
| --- | --- | --- |
| 玩家 | 单帧 64×64 或 96×96 | 待机、移动、受击；每动作 4–6 帧，按实际设计决定方向数 |
| 普通怪物 | 单帧 48×48 至 96×96 | 移动、受击、死亡 |
| Boss | 单帧 128×128 至 256×256 | 待机、移动、攻击、受击、死亡 |
| 枪械 | 32×16 至 96×48 | 独立于人物，标注旋转中心和枪口坐标 |
| 子弹 | 8×8 至 24×24 | 可静态，但运动轨迹和朝向必须正确 |
| 环刃 | 32×32 至 64×64 | 旋转兼容，中心锚点明确，可选 4–6 帧能量变化 |
| 落雷 | 按效果区域设计 | 预警、落雷、命中三个阶段，不使用单张长图平移 |
| 塔罗牌 | 统一卡面尺寸 | UI 素材，不参与世界场景移动 |

### 5.3 单项素材申请模板

```text
系统/对象：
玩法状态：占位版本是否已验收
素材用途：
目标显示尺寸：
单帧尺寸：
动作列表：
每个动作帧数：
方向数量：
锚点位置：
枪口/发射点：
效果持续时间：
色板与风格参考：
性能预算：
验收截图或录像要求：
```

## 6. 每次改动的完成定义

一个改动只有同时满足以下条件，才算完成：

1. 改动范围单一，能够用一句话描述；
2. Godot 4.7.1 可以解析项目和相关场景；
3. 没有新增缺失资源、无效 UID 或脚本错误；
4. 核心场景能够启动并完成对应功能测试；
5. 涉及触摸操作时，至少验证横屏触摸逻辑和安全区；
6. 新增的子弹、特效、敌人和音频实例都有释放或复用机制；
7. 不提交缓存、导出包、密钥、账号信息或临时文件；
8. 必要时同步更新本流程、README 或变更记录；
9. 检查通过后提交并推送到远程仓库。

## 7. Git 自动提交与推送规则

后续按原子改动执行以下流程：

1. 检查 `git status` 和实际差异；
2. 运行当前阶段适用的解析、启动或自动化检查；
3. 确认没有缓存、敏感信息和无关文件；
4. 使用清晰的 Conventional Commit 信息提交；
5. 自动推送到 `origin/main`；
6. 向用户报告提交哈希、验证结果和下一阶段内容。

提交示例：

```text
docs: define staged Godot rebuild workflow
refactor: replace runtime artwork with placeholders
fix: remove player movement drift
feat: fire projectiles from weapon muzzle
feat: add placeholder blade weapon
art: add animated player sprite set
perf: pool projectiles and temporary effects
```

以下情况暂停自动推送并先报告：

- 测试或解析失败；
- 远程分支出现非本机提交并产生冲突；
- 需要删除无法确认归属的用户文件；
- 发现密钥、令牌或其他敏感信息；
- 改动会改变既定玩法范围，需要用户决定。

## 8. 下一步

下一次改动只执行“阶段 0：基线确认与视觉清理”：移除 Godot 运行时对现有静态人物、怪物、枪械和技能图片的依赖，使用几何占位图形恢复一个干净、可运行、便于逐项打磨的战斗基线。该阶段不会生成或引入任何新图片。
